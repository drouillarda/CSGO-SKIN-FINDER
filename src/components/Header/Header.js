import "./Header.scss"
import { Link } from "react-router-dom";

export const Header = () => {
    return (
        <header className="nav">
            <h5>CSGO SKIN FINDER</h5>
        </header>
    );
};